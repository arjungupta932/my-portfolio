import { motion } from "framer-motion";
import { certificatesData } from "../data/portfolioData";
function Certificates() { return <section id="certificates" className="px-5 py-24 lg:px-8"><div className="mx-auto max-w-7xl"><div className="text-center"><p className="eyebrow">Learning & credentials</p><h2 className="section-title">Always growing the toolkit.</h2></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{certificatesData.map((certificate, index) => { const Icon = certificate.icon; return <motion.div key={certificate.title} initial={{ opacity: 0, scale: .92 }} whileInView={{ opacity: 1, scale: 1 }} viewport={{ once: true }} transition={{ delay: index * .08 }} whileHover={{ y: -5 }} className="glass-card p-6"><Icon className="text-3xl text-cyan-300" /><h3 className="mt-5 font-bold text-white">{certificate.title}</h3><p className="mt-2 text-sm text-slate-400">Current learning focus</p></motion.div>; })}</div></div></section>; }
export default Certificates;
